# Copy this file to .env.local (which is gitignored) and fill in the values
# from your Firebase project: console.firebase.google.com → Project settings
# → General → Your apps → SDK setup and configuration → "Config".
#
# In the Firebase console you also need to enable, under Authentication → Sign-in method:
#   - Email/Password
#   - Google
# And add your local dev origin (http://localhost:3000) plus your deployed
# domain under Authentication → Settings → Authorized domains.

REACT_APP_FIREBASE_API_KEY=
REACT_APP_FIREBASE_AUTH_DOMAIN=
REACT_APP_FIREBASE_PROJECT_ID=
REACT_APP_FIREBASE_STORAGE_BUCKET=
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=
REACT_APP_FIREBASE_APP_ID=
